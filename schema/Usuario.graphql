type Perfil {
        id: Int
        name: String
    }

enum UsuarioStatus {
    ATIVO
    INATIVO
    BLOQUEADO
}

type Usuario {
    id: ID
    name: String!
    email: String!
    idade: Int
    salario: Float
    vip: Boolean
    perfil: Perfil
    status: UsuarioStatus
}