type Query {
    ola: String
    createdAt: String
    horaAtual: Date
    usuarioLogado: Usuario
    produtoEmDestaque: Produto
    numeroMegaSena: [Int!]!
    usuarios: [Usuario!]!
    usuario(id: ID): Usuario
    perfis: [Perfil]
    perfil(id: Int): Perfil
}