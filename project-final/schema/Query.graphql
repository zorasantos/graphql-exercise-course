# Pontos de entrada da sua API!
type Query {
    login(data: UserLoginInput!): User
    users: [User]
    user(filter: UserFilter!): User
    perfis: [Perfil]
    perfil(filter: PerfilFilter!): Perfil
}